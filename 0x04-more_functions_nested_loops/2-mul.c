#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies numbers inputted
 * @a : input number
 * @b : input number
 * Return : the result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
