#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies numbers inputted
 * @a : input number
 * @b : input number
 * Return: multiplication of the numbers.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
